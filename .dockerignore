# Build artifacts and deps are produced inside the image.
node_modules
dist
dist-ssr

# VCS / CI / tooling — not needed at build or runtime.
.git
.github
.claude

# Secrets and local cruft.
.env
.env.*
!.env.example
.DS_Store

# Docs.
*.md
